#include "common/types.h"

std::vector<const char *> FILE_MODES = { "r", "w", "a" };

std::vector<const char *> SOURCE_TYPES = { "static", "stream" };

std::vector<const char *> DRAW_MODES = { "fill", "line" };

std::vector<const char *> KEYBOARD_TYPES = { "standard", "basic", "numpad"};