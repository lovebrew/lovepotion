language: cpp

sudo: required

before_install:
  - sudo apt-get update && sudo apt-get install p7zip-full
  - export CC=aarch64-none-elf-gcc CXX=aarch64-none-elf-g++
  - export DEVKITPRO=${HOME}/devkitPro
  - export DEVKITA64=${DEVKITPRO}/devkitA64
  - mkdir ${DEVKITPRO} && mkdir ${DEVKITA64}
  - wget https://raw.githubusercontent.com/devkitPro/installer/master/perl/devkitA64update.pl -O devkitA64update.pl
  - chmod +x ./devkitA64update.pl
  - ./devkitA64update.pl

script:
  - make

after_success:
  - wget https://raw.githubusercontent.com/k3rn31p4nic/travis-ci-discord-webhook/master/send.sh
  - chmod +x send.sh
  - ./send.sh success $WEBHOOK_URL

after_failure:
  - wget https://raw.githubusercontent.com/k3rn31p4nic/travis-ci-discord-webhook/master/send.sh
  - chmod +x send.sh
  - ./send.sh failure $WEBHOOK_URL
