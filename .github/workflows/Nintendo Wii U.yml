name: Nintendo Wii U

on: [push, pull_request]

jobs:
    build:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                config: [release, debug]

        container:
            image: docker://devkitpro/devkitppc
            options: --cpus 2

        steps:
            - name: checkout
              uses: actions/checkout@v7

            - name: compile
              run: catnip -T WiiU ${{ matrix.config }}

            - id: commit
              run: echo "short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

            - uses: actions/upload-artifact@v7
              with:
                  name: Nintendo Wii U-${{ steps.commit.outputs.short }} (${{ matrix.config }})
                  path: |
                      build/**/*.elf
                      build/**/*.wuhb
